next-version: 6.0.0
branches:
  develop:
    tag: beta
    mode: ContinuousDeployment
  release:
    tag: rc
    mode: ContinuousDeployment
assembly-file-versioning-format: '{Major}.{Minor}.{Patch}.{env:GITHUB_RUN_NUMBER ?? 0}'
